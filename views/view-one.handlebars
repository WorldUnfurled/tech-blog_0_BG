<div class="single-post">
  <article class="card">
    <div class="card-header">
      <h2>{{post.title}}</h2>
      <div>
        Posted by {{post.user.username}} on {{format_date post.createdAt}}
      </div>
    </div>
    <div class="card-content">
      {{post.content}}
    </div>
  </article>

  <form id="new-comment" class="card">
    <div class="card-header">
      <h3>Add a comment!</h3>
    </div>

    <div class="card-content">
      <input type="hidden" id="comment-post-id" name="post-id" value="{{post.id}}" />

      <label for="comment-content">Comment</label>

      <textarea name="comment-content" id="comment-content" class="form-input"></textarea>

      <button type="submit" class="btn">Submit</button>
    </div>
  </form>

    <section class="card">
      <div class="card-header">
        <h3>Comments (Be Nice!)</h3>
      </div>

      <div class="card-content">
        {{#each post.comments}}
          <div>
            <p>
              {{content}}
            </p>
            <p>
              &mdash; {{user.username}}, {{format_date createdAt}}
            </p>
          </div>
        {{/each}}
      </div>
    </section>
</div>

<script src="/js/comment.js"></script>